interface:
  display_name: "Convert GURPS Monsters to GCS"
  short_description: "Extract fan bestiaries and build GCS races"
  default_prompt: "Use $convert-gurps-monsters-to-gcs to extract a GURPS monster bestiary, match it to parsed SRDs, and build reviewable GCS race templates."
